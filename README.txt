
总括：
	db: mysql8.0
	jdk: 8

每个项目介绍：
scm:
	SpringMVC + mybatis  前后端不分离（也可前后端分离，直接返回json数据，不返回视图即可），前端easyui + jsp，也可提供restful接口返回json格式
	
sbcm:
	Springboot + SpringMVC + mybatis 前后端分离（也可前后端不分离，webapp下放html即可），支持自动重启（idea）和LiveReload